# RandomForestRegressor outputs

This folder contains Sam's saved RandomForestRegressor output evidence used by the Streamlit application. The app reads these files directly and does not retrain the model.
